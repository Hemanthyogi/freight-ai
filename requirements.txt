# ============================================================
# FreightMind AI — Python Dependencies (Vercel Serverless)
# ============================================================
# IMPORTANT: Keep this minimal — Vercel has a 500 MB function limit.
# Heavy ML packages (pandas, scikit-learn, xgboost, lightgbm, scipy,
# statsmodels, shap) must NOT be listed here.
# The Vercel API (api/index.py) serves pre-computed demo responses only.
# ============================================================

fastapi>=0.110.0
uvicorn[standard]>=0.28.0
pydantic>=2.0.0
python-dotenv>=1.0.0
pyyaml>=6.0.0
loguru>=0.7.0
