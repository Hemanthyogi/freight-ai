# ============================================================
# FreightMind AI — Full Python Dependencies (Railway / Local)
# ============================================================
# Use this file for Railway, local development, and CI/CD.
# DO NOT use this on Vercel — use requirements.txt instead.
# ============================================================

# --- Core Data & Math ---
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0

# --- Machine Learning ---
scikit-learn>=1.3.0
xgboost>=2.0.0
lightgbm>=4.0.0
statsmodels>=0.14.0

# --- Explainability ---
shap>=0.45.0

# --- Backend API ---
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
pydantic>=2.0.0
python-dotenv>=1.0.0
aiofiles>=23.0.0

# --- Database ---
sqlalchemy>=2.0.0
aiosqlite>=0.20.0

# --- Serialization & Utils ---
joblib>=1.3.0
pyyaml>=6.0.0
python-dateutil>=2.8.0
tqdm>=4.66.0
loguru>=0.7.0

# --- Background Scheduling ---
apscheduler>=3.10.0

# --- Visualization (EDA / Reports only) ---
matplotlib>=3.7.0
seaborn>=0.12.0
